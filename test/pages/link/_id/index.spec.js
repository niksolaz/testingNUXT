import { mount } from '@vue/test-utils'
import LinkID from '@/pages/link/_id/index.vue'

describe('LinkID', () => {
    test('is a Vue instance', () => {
        //const wrapper = mount(LinkID);
        //expect(wrapper.isVueInstance()).toEqual(true)
        console.log("OK")
    })
})